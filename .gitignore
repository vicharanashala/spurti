node_modules/
dist/
.env
.env.*
!.env.example
!.env.ssh.example
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Student data is NOT source and must never be committed — names, emails,
# attendance, poll responses, SP ledgers and generated cards are personal data.
# Once a file like this is in history it stays there, so the patterns are broad
# on purpose.
data/
server/data/
*.csv
*.xlsx

# editor backups
*.bak
*.bak.*

# pipeline secrets — never commit
pipeline/.env

# runtime log + local DB backups (not for the repo)
server.log
sp-runs/

# student data — LOCAL ONLY, never commit (untracked 2026-07-14)
data/

# server-side run output
logs/
sp-runs/
*.bak-*
.spurti-plan/
