# Workspace Rules

- When the user asks to run the project locally, start the local server if it is not already running, and automatically open the application in the browser at `http://localhost:5290`.
